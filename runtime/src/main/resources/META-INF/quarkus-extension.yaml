name: Quarkus Extension Status
description: Exposes ApiStatus endpoint to monitor the API status (uses last 2 parts of quarkus.http.root-path as serviceName and version)
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  keywords:
    - quarkus-extension-status
#  guide: ... # To create and publish this guide, see https://github.com/quarkiverse/quarkiverse/wiki#documenting-your-extension
  categories:
   - "miscellaneous"
  status: "experimental"
